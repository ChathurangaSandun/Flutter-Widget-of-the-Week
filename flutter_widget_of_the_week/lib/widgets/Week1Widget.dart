import 'package:flutter/material.dart';

class Week1Widget extends StatelessWidget {
  static const routeName = "weekone";
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Widget 1"),);
  }
}
